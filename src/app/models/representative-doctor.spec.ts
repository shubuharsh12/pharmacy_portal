import { RepresentativeDoctor } from './representative-doctor';

describe('RepresentativeDoctor', () => {
  it('should create an instance', () => {
    expect(new RepresentativeDoctor()).toBeTruthy();
  });
});
